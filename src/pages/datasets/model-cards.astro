---
import manifest from "@generated/vault-manifest.json";
import VaultLayout from "@layouts/VaultLayout.astro";

const modelCards = (manifest.nodes || [])
	.filter((node) => node.type === "dataset" && node.datasetKind === "model-card")
	.slice()
	.sort((a, b) => {
		const aDate = a.updated ? new Date(a.updated) : new Date(0);
		const bDate = b.updated ? new Date(b.updated) : new Date(0);
		return bDate.valueOf() - aDate.valueOf();
	});
---

<VaultLayout
	title="Dataset Model Cards — phi9.space"
	description="Addressable model card index sourced from vault datasets."
>
  <section>
    <p class="section-kicker">Datasets</p>
    <h1>Model Cards</h1>
    <p class="section-intro">Direct index for dataset model cards from vault.</p>
  </section>

  {
    modelCards.length > 0 ? (
      <section class="note-grid">
        {modelCards.map((entry) => (
          <article class="note-card">
            <h3>
              <a href={`/datasets/${entry.slug}/`}>{entry.title}</a>
            </h3>
            <p>{entry.excerpt}</p>
            <div class="note-card__meta">
              <span>{entry.category}</span>
              {entry.updated && <span>{new Date(entry.updated).toLocaleDateString()}</span>}
            </div>
          </article>
        ))}
      </section>
    ) : (
      <p>No model cards yet. See <a href="/lab/publishing">/lab/publishing</a> for the publishing workflow.</p>
    )
  }
</VaultLayout>

<style>
  .section-kicker {
    margin: 0;
    font-size: 0.74rem;
    font-weight: 700;
    letter-spacing: 0.13em;
    text-transform: uppercase;
    color: var(--color-accent);
  }

  .section-intro {
    margin-top: 0.35rem;
    color: var(--color-muted);
  }
</style>
