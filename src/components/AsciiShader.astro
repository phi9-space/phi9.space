<div class="ascii-shader">
  <div class="ascii-shader__overlay">
    <span class="ascii-shader__label">ascii shader coming soon</span>
  </div>
</div>

<style>
  .ascii-shader {
    position: absolute;
    inset: -12vh;
    background: radial-gradient(circle at 18% 20%, rgba(255, 120, 48, 0.18), transparent 60%),
      radial-gradient(circle at 82% 78%, rgba(255, 180, 102, 0.16), transparent 62%),
      var(--color-background);
    opacity: 0.5;
    filter: blur(140px);
    transform: scale(1.05);
  }

  .ascii-shader__overlay {
    position: absolute;
    inset: 0;
    background-image: linear-gradient(
        rgba(252, 247, 233, 0.92),
        rgba(252, 247, 233, 0.88)
      ),
      repeating-linear-gradient(
        to bottom,
        rgba(255, 140, 70, 0.15) 0,
        rgba(255, 140, 70, 0.15) 1px,
        transparent 1px,
        transparent 8px
      );
    mix-blend-mode: normal;
    display: flex;
    align-items: flex-end;
    justify-content: flex-end;
    padding: 1.5rem 2rem;
  }

  .ascii-shader__label {
    font-size: 0.75rem;
    letter-spacing: 0.3em;
    text-transform: uppercase;
    color: rgba(18, 18, 18, 0.22);
  }

  @media (max-width: 768px) {
    .ascii-shader {
      opacity: 0.2;
      filter: blur(120px);
    }
  }
</style>
