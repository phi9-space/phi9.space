---
interface Props {
	width?: string;
	height?: string;
	color?: string;
	class?: string;
}

const {
	width = "100%",
	height = "12rem",
	color = "var(--color-text)",
	class: className = "",
} = Astro.props;
---

<div
	class:list={["shape shape--rect", className]}
	style={`--shape-w: ${width}; --shape-h: ${height}; --shape-color: ${color};`}
	aria-hidden="true"
></div>

<style>
	.shape--rect {
		width: var(--shape-w);
		height: var(--shape-h);
		background: var(--shape-color);
	}
</style>
