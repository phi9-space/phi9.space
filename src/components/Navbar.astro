---
import { contactMailto, primaryNavLinks } from "@lib/site";

const currentPath = Astro.url.pathname.replace(/\/$/, "") || "/";
---

<div class="site-nav">
  <a class="site-nav__brand" href="/" aria-label="phi9.space home">
    <img
      src="/assets/brand/phi9-lockup-black.svg"
      alt="phi9.space"
      class="site-nav__logo site-nav__logo--desktop site-nav__logo--light"
      loading="lazy"
    />
    <img
      src="/assets/brand/phi9-lockup-white.svg"
      alt=""
      aria-hidden="true"
      class="site-nav__logo site-nav__logo--desktop site-nav__logo--dark"
      loading="lazy"
    />
    <img
      src="/assets/brand/phi9-mark-black.svg"
      alt="phi9.space"
      class="site-nav__logo site-nav__logo--mobile site-nav__logo--light"
      loading="lazy"
    />
    <img
      src="/assets/brand/phi9-mark-white.svg"
      alt=""
      aria-hidden="true"
      class="site-nav__logo site-nav__logo--mobile site-nav__logo--dark"
      loading="lazy"
    />
  </a>

  <button
    type="button"
    class="site-nav__toggle"
    data-nav-toggle
    aria-label="Toggle navigation menu"
    aria-expanded="false"
    aria-controls="site-nav-menu"
  >
    <span class="site-nav__toggle-lines" aria-hidden="true">
      <span></span>
      <span></span>
      <span></span>
    </span>
  </button>

  <nav aria-label="Primary" id="site-nav-menu" class="site-nav__menu" data-nav-menu>
    <ul>
      {primaryNavLinks.map((item) => {
        const normalizedHref = item.href.replace(/\/$/, "") || "/";
        const prefixMatch =
          currentPath === normalizedHref ||
          (normalizedHref !== "/" && currentPath.startsWith(`${normalizedHref}/`));
        const shadowed = prefixMatch && primaryNavLinks.some((other) => {
          const otherHref = other.href.replace(/\/$/, "") || "/";
          return otherHref !== normalizedHref
            && otherHref.startsWith(`${normalizedHref}/`)
            && (currentPath === otherHref || currentPath.startsWith(`${otherHref}/`));
        });
        const active = prefixMatch && !shadowed;

        return (
          <li>
            <a href={item.href} data-active={active ? "true" : "false"}>
              {item.label}
            </a>
          </li>
        );
      })}
    </ul>
    <div class="site-nav__actions">
      <button
        type="button"
        class="site-nav__theme"
        data-theme-toggle
        aria-label="Toggle theme"
        title="Toggle theme"
      >
        <svg class="site-nav__theme-icon site-nav__theme-icon--light" viewBox="0 0 24 24" aria-hidden="true">
          <circle cx="12" cy="12" r="4.5" fill="none" stroke="currentColor" stroke-width="1.7" />
          <path d="M12 2.5v2.5M12 19v2.5M4.4 4.4l1.8 1.8M17.8 17.8l1.8 1.8M2.5 12H5M19 12h2.5M4.4 19.6l1.8-1.8M17.8 6.2l1.8-1.8" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" />
        </svg>
        <svg class="site-nav__theme-icon site-nav__theme-icon--dark" viewBox="0 0 24 24" aria-hidden="true">
          <path d="M20.5 14.5A8 8 0 0 1 9.5 3.5a0.5 0.5 0 0 0-0.6-0.6A9.5 9.5 0 1 0 21.1 15.1a0.5 0.5 0 0 0-0.6-0.6Z" fill="currentColor" />
        </svg>
      </button>
      <a href={contactMailto} class="site-nav__cta">Get in touch</a>
    </div>
  </nav>
</div>

<script is:inline>
  const navRoot = document.querySelector(".site-nav");
  const navToggle = navRoot?.querySelector("[data-nav-toggle]");
  const navMenu = navRoot?.querySelector("[data-nav-menu]");
  const themeToggle = navRoot?.querySelector("[data-theme-toggle]");
  const navLinks = navMenu ? Array.from(navMenu.querySelectorAll("a")) : [];

  const closeMenu = () => {
    if (!navRoot || !navToggle) return;
    navRoot.setAttribute("data-menu-open", "false");
    navToggle.setAttribute("aria-expanded", "false");
  };

  const openMenu = () => {
    if (!navRoot || !navToggle) return;
    navRoot.setAttribute("data-menu-open", "true");
    navToggle.setAttribute("aria-expanded", "true");
  };

  navToggle?.addEventListener("click", () => {
    const isOpen = navRoot?.getAttribute("data-menu-open") === "true";
    if (isOpen) closeMenu();
    else openMenu();
  });

  for (const link of navLinks) {
    link.addEventListener("click", closeMenu);
  }

  window.addEventListener("resize", () => {
    if (window.innerWidth > 800) closeMenu();
  });

  themeToggle?.addEventListener("click", () => {
    const current = document.documentElement.getAttribute("data-theme") === "dark" ? "dark" : "light";
    const next = current === "dark" ? "light" : "dark";
    document.documentElement.setAttribute("data-theme", next);
    try {
      localStorage.setItem("phi9-theme", next);
    } catch (e) {
      // storage unavailable — theme still applied this session
    }
  });
</script>

<style>
  .site-nav {
    max-width: var(--max-width);
    margin: 0 auto;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1.25rem;
    padding: 0.75rem clamp(1rem, 3vw, 2rem);
  }

  .site-nav__brand {
    display: inline-flex;
    align-items: center;
    flex-shrink: 0;
  }

  .site-nav__logo {
    height: auto;
    display: block;
  }

  .site-nav__logo--desktop {
    width: clamp(138px, 16vw, 192px);
  }

  .site-nav__logo--mobile {
    display: none;
    width: 1.75rem;
  }

  .site-nav__logo--dark {
    display: none;
  }

  [data-theme="dark"] .site-nav__logo--light {
    display: none;
  }

  [data-theme="dark"] .site-nav__logo--dark.site-nav__logo--desktop {
    display: block;
  }

  [data-theme="dark"] .site-nav__logo--dark.site-nav__logo--mobile {
    display: none;
  }

  .site-nav__toggle {
    display: none;
    border: 1px solid var(--color-border);
    background: color-mix(in srgb, var(--color-bg) 82%, transparent);
    color: var(--color-text);
    border-radius: 999px;
    width: 2.75rem;
    height: 2.75rem;
    padding: 0;
    align-items: center;
    justify-content: center;
    cursor: pointer;
    backdrop-filter: blur(16px);
    -webkit-backdrop-filter: blur(16px);
    box-shadow: 0 10px 24px color-mix(in srgb, var(--color-text) 8%, transparent);
    transition:
      background 0.2s ease,
      border-color 0.2s ease,
      color 0.2s ease,
      transform 0.2s ease;
  }

  .site-nav__toggle:hover {
    border-color: color-mix(in srgb, var(--color-accent) 50%, var(--color-border) 50%);
    color: var(--color-accent);
  }

  .site-nav__toggle-lines {
    position: relative;
    display: block;
    width: 1rem;
    height: 0.8rem;
  }

  .site-nav__toggle-lines span {
    position: absolute;
    left: 0;
    width: 100%;
    height: 1.5px;
    border-radius: 999px;
    background: currentColor;
    transition:
      transform 0.2s ease,
      opacity 0.2s ease,
      top 0.2s ease;
  }

  .site-nav__toggle-lines span:nth-child(1) {
    top: 0;
  }

  .site-nav__toggle-lines span:nth-child(2) {
    top: calc(50% - 0.75px);
  }

  .site-nav__toggle-lines span:nth-child(3) {
    top: calc(100% - 1.5px);
  }

  .site-nav__menu {
    display: flex;
    align-items: center;
    gap: 2rem;
  }

  .site-nav__actions {
    display: flex;
    align-items: center;
    gap: clamp(0.75rem, 1.5vw, 1.25rem);
  }

  .site-nav__theme {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 2.25rem;
    height: 2.25rem;
    padding: 0;
    border: 1px solid var(--color-border);
    border-radius: 999px;
    background: transparent;
    color: var(--color-muted);
    cursor: pointer;
    transition:
      color 0.2s ease,
      border-color 0.2s ease,
      background 0.2s ease;
  }

  .site-nav__theme:hover {
    color: var(--color-accent);
    border-color: color-mix(in srgb, var(--color-accent) 45%, var(--color-border) 55%);
  }

  .site-nav__theme-icon {
    width: 1.05rem;
    height: 1.05rem;
  }

  .site-nav__theme-icon--dark {
    display: none;
  }

  [data-theme="dark"] .site-nav__theme-icon--light {
    display: none;
  }

  [data-theme="dark"] .site-nav__theme-icon--dark {
    display: block;
  }

  .site-nav__menu ul {
    list-style: none;
    display: flex;
    align-items: center;
    gap: clamp(1rem, 2.5vw, 2rem);
    margin: 0;
    padding: 0;
  }

  .site-nav__menu a {
    font-family: var(--font-heading);
    font-size: 0.9375rem;
    font-weight: 500;
    color: var(--color-text);
    text-decoration: none;
    transition: color 0.15s ease;
  }

  .site-nav__menu a:hover {
    color: var(--color-accent);
    text-decoration: none;
  }

  .site-nav__menu a[data-active="true"] {
    color: var(--color-accent);
  }

  .site-nav__cta {
    font-family: var(--font-heading);
    font-size: 0.9375rem;
    font-weight: 500;
    color: var(--color-accent) !important;
    white-space: nowrap;
    transition: color 0.15s ease;
  }

  .site-nav__cta:hover {
    color: var(--color-accent-hover) !important;
    text-decoration: none !important;
  }

  @media (max-width: 800px) {
    .site-nav {
      display: grid;
      grid-template-columns: 1fr auto;
      grid-template-areas:
        "brand toggle"
        "menu menu";
      align-items: center;
      gap: 0.25rem 0.85rem;
      height: auto;
      margin: 0.6rem 0.8rem 0;
      padding: 0.75rem 0.85rem;
      background: color-mix(in srgb, var(--color-bg) 76%, transparent);
      border: 1px solid color-mix(in srgb, var(--color-border) 82%, transparent);
      border-radius: 1.25rem;
      backdrop-filter: blur(22px);
      -webkit-backdrop-filter: blur(22px);
      box-shadow: 0 18px 40px rgba(22, 22, 29, 0.09);
    }

    .site-nav__brand {
      grid-area: brand;
    }

    .site-nav__logo--desktop {
      display: none;
    }

    .site-nav__logo--mobile {
      display: block;
    }

    .site-nav__toggle {
      display: inline-flex;
      grid-area: toggle;
      justify-self: end;
    }

    .site-nav__menu {
      grid-area: menu;
      display: none;
      flex-direction: column;
      align-items: flex-start;
      gap: 1rem;
      margin-top: 0.65rem;
      padding-top: 0.9rem;
      border-top: 1px solid color-mix(in srgb, var(--color-border) 88%, transparent);
      width: 100%;
    }

    .site-nav[data-menu-open="true"] .site-nav__menu {
      display: flex;
    }

    .site-nav[data-menu-open="true"] {
      background: color-mix(in srgb, var(--color-bg) 88%, transparent);
      border-color: color-mix(in srgb, var(--color-accent) 22%, var(--color-border) 78%);
    }

    .site-nav[data-menu-open="true"] .site-nav__toggle {
      background: color-mix(in srgb, var(--color-accent-soft) 72%, var(--color-bg) 28%);
      border-color: color-mix(in srgb, var(--color-accent) 36%, var(--color-border) 64%);
      color: var(--color-accent);
      transform: rotate(90deg);
    }

    .site-nav[data-menu-open="true"] .site-nav__toggle-lines span:nth-child(1) {
      top: calc(50% - 0.75px);
      transform: rotate(45deg);
    }

    .site-nav[data-menu-open="true"] .site-nav__toggle-lines span:nth-child(2) {
      opacity: 0;
    }

    .site-nav[data-menu-open="true"] .site-nav__toggle-lines span:nth-child(3) {
      top: calc(50% - 0.75px);
      transform: rotate(-45deg);
    }

    .site-nav__menu ul {
      display: flex;
      flex-direction: column;
      gap: 0.7rem;
      align-items: flex-start;
      width: 100%;
    }

    .site-nav__menu li,
    .site-nav__menu a {
      width: 100%;
    }

    .site-nav__menu a {
      font-size: 1rem;
    }

    .site-nav__cta {
      margin-top: 0.15rem;
      padding-top: 0.45rem;
    }
  }
</style>
